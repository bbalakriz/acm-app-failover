---
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSetBinding
metadata:
  name: default
  namespace: default # Same as the namespace of the Placement
spec:
  clusterSet: default # Match the ManagedClusterSet name
